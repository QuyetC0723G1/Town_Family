import manager.TownManager;
import model.Family;
import model.Person;
import view.Menu;

class Main{
    public static void main(String[] args) {
        Menu menu = new Menu();
        menu.showMenu();
    }
}